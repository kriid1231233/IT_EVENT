import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HistorialdeeventosPage } from './historialdeeventos.page';

describe('HistorialdeeventosPage', () => {
  let component: HistorialdeeventosPage;
  let fixture: ComponentFixture<HistorialdeeventosPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(HistorialdeeventosPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
